<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
        <td><%= link_to 'Show', book, class: "show_#{book.id}" %></td>
        <!--なぜshowはパスの指定も（editで実験したがpathの指定がないと飛ばない）idの指定もないのにちゃんと飛ぶのか-->
        <!--メンターさんによるとshowアクションは複数のbookのpathで複数のメソッドをもつので後ろにメソッドを指定していなければget、メソッドをしていればそれぞれのアクションにとぶように-->
        <!--なっており、こういったものは_pathやIDの指定を省略しても希望通りの動きになるらしい-->
        <td><%= link_to 'Edit', edit_book_path(book.id), class: "edit_#{book.id}" %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' }, class: "destroy_#{book.id}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<h2>New book</h2>
<%= render 'form', book:@book %>
